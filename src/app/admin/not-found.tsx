import { AdminNotFoundView } from "@/components/NotFoundViews";

/** The admin not-found boundary; the routes render the same body themselves. */
export default function AdminNotFound() {
  return <AdminNotFoundView />;
}
